beforeEach(() => {
  cy.visit('http://localhost:3000');
});
// TODO:
it('should can write data to firebase', () => {});
it('should start timer and then add new timer then start that timer again', () => {});
